export interface InstallOptions {
    destLoc: string;
    isCleanInstall?: boolean;
    isStrict?: boolean;
    isOptimized?: boolean;
    skipFailures?: boolean;
    namedExports?: {
        [filepath: string]: string[];
    };
}
export declare function install(arrayOfDeps: string[], { isCleanInstall, destLoc, skipFailures, isStrict, isOptimized, namedExports }: InstallOptions): Promise<boolean>;
export declare function cli(args: string[]): Promise<void>;
